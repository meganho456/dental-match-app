DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/dental_match?schema=public"
PORT=3001

# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="change-me-to-a-random-64-char-secret"
JWT_EXPIRES_IN="7d"
